services:
  backend:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - db

  db: 
    image: postgres 
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: testdb
    volumes:
      - ./db-init:/docker-entrypoint-initdb.d
